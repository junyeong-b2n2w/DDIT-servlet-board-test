<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE sqlMap
	PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">
	

<sqlMap namespace="reply">
	<insert id="replySave" parameterClass="replyVO">
		INSERT INTO reply(renum, bonum, name, cont, redate)
		 values (reply_renum_seq.nextval , #bonum#,#name#,#cont#,sysdate)
		 
		 <selectKey keyProperty="renum" resultClass="int">
		 	SELECT reply_renum_seq.currentval FROM dual; 
		 </selectKey>
		  
	</insert>
	
</sqlMap>